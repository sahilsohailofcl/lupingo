# .npmrc (in the root of your monorepo)

# The following flag forces a flat node_modules structure, which is often required 
# by older or complex frameworks (like Next.js/React in monorepos) to prevent 
# "Invalid Hook Call" errors caused by duplicate React installations.
shamefully-hoist=true